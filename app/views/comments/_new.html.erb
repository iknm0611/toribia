  <%= form_with(model: @comment, url: post_comments_path(post_id: @post.id), method: :post) do |f| %>
  <%= f.text_area :comment %>
  <%= f.hidden_field :post_id, value: @post.id %>
  <%= f.submit 'コメントする' %>
  <% end %>